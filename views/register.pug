extend layout
block content
  .form-container
    form(class="form-signin" method="POST" action="/auth/register")
      p(class = "error")=message
      h1(class = "head") Chula Democracy
      .form-group
        label( for="username" class="sr-only") Username
        input(type="text" name="username" id="username" class="form-control" placeholder="Student ID" required)
      .form-group
        label( for="nationalID" class="sr-only") National ID
        input(type="text" name="nationalID" id="nationalID" class="form-control" placeholder="National ID" required)
      .form-group
        label(for="password" class="sr-only") Password
        input(type="password" name="password" id="password" class="form-control" placeholder="Password" required)
      .form-group
        label(for="password" class="sr-only") Confirm Password
        input(type="password" name="confirmPassword" id="confirmPassword" class="form-control" placeholder="Confirm Password" required)
      button(class="btn btn-primary btn-block" type="submit") Register
      a(href="/login", class="btn btn-link") Have an account?